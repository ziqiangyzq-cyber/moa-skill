<!-- End-B fix: cross-vendor review of the FINAL merge. MUST be routed to a worker whose
     vendor differs from the aggregator's. This is a check on the final answer, NOT a third
     round — if it flags real distortion, the aggregator makes ONE bounded revision and stops. -->

Below is a PROPOSED FINAL ANSWER produced by merging several experts' views, followed by the
raw disagreements it claims to have resolved. You are from a **different model lineage** than
the author of the merge — your job is to catch what that lineage's bias let through.

Answer only this:
- Did the merge **distort** any expert's position, **drop** a point that mattered to the
  decision, or resolve a real disagreement by **asserting** rather than by reasoning?
- Does the final answer show a **lineage bias** — a default the merging model would hold that a
  different vendor's model would challenge?
- Is any load-bearing claim **unverifiable** (training-consensus only, no evidence)?

If the merge is faithful and you have nothing material, reply exactly: `NOTHING TO ADD`.
Otherwise, list the specific distortions/omissions concisely. Do not rewrite the answer.

=== PROPOSED FINAL ANSWER ===
{{FINAL}}

=== RAW DISAGREEMENTS IT CLAIMS TO RESOLVE ===
{{DISAGREEMENTS}}
